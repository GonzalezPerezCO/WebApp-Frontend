import React from 'react';
import Form from './Form';

class Login extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <Form />;
  }
}

export default Login;
